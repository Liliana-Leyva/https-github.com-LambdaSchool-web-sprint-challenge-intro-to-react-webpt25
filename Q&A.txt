1. What is React JS and what problems does it solve? Support your answer with concepts introduced in class and from your personal research on the web.
It is a javascript library created by facebook for building user interfaces. React has different componets and we use those components to tell React what 
we want to see the screen. It has a render method that return a React Element and displays the result. 


2. Describe component state.
It is an object that stores components. It is dynamic and keeps track of any information that changes  and will call the render method each time 
an update happens. 

3. Describe props.
It is a property and stands and is used to pass data from one component to another. The data is passed from parent to child.

4. What are side effects, and how do you sync effects in a React component to changes of certain state or props?
It is anything that affects something outside the scope of the current function that is being executed, like an API request.
We can sying effects using useEffect.

You are expected to be able to answer questions in these areas. Your responses contribute to your Sprint Challenge grade.
